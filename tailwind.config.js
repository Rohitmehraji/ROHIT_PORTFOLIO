/**
 * Tailwind is intentionally not used by the current UI implementation.
 * Keeping this file minimal avoids config-related startup errors if Tailwind is installed.
 */
export default {
  content: [],
  theme: { extend: {} },
  plugins: []
}
